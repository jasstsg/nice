{
  "name": "@jasstsg/nice",
  "version": "0.1.0",
  "description": "Node Integrated Content Editor - a tiny schema-driven local content editor that reads and writes plain JSON files.",
  "bin": {
    "nice": "build/bin/nice.js"
  },
  "main": "build/lib/content-helpers.js",
  "types": "build/lib/content-helpers.d.ts",
  "files": [
    "build",
    "dist"
  ],
  "scripts": {
    "start": "node build/bin/nice.js",
    "dev": "vite",
    "build:server": "tsc -p tsconfig.server.json",
    "build:frontend": "vite build",
    "build:demo": "rm -rf demo/node_modules/nice && cd demo && npm install",
    "build": "npm run build:server && npm run build:frontend && npm run build:demo",
    "deploy": "npm version patch && npm run build && npm publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jasstsg/nice.git"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
